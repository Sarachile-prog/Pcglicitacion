
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    function isSignedIn() {
      return request.auth != null;
    }

    function isSuperAdmin() {
      return isSignedIn() && (
        request.auth.token.email == 'control@pcgoperacion.com' ||
        (exists(/databases/$(database)/documents/users/$(request.auth.uid)) && 
         get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'SuperAdmin')
      );
    }

    match /users/{userId} {
      allow get: if isSignedIn() && (request.auth.uid == userId || isSuperAdmin());
      allow list: if isSuperAdmin();
      allow create: if isSignedIn() && request.auth.uid == userId;
      allow update: if isSignedIn() && (request.auth.uid == userId || isSuperAdmin());
    }

    match /companies/{companyId} {
      allow read, write: if isSuperAdmin() || (isSignedIn() && exists(/databases/$(database)/documents/users/$(request.auth.uid)) && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.companyId == companyId);
      
      match /bookmarks/{bidId} {
        allow read, write: if isSuperAdmin() || (isSignedIn() && exists(/databases/$(database)/documents/users/$(request.auth.uid)) && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.companyId == companyId);
      }
    }

    match /support_tickets/{ticketId} {
      allow create: if isSignedIn();
      // Optimizamos el listado para permitirlo si el filtro coincide con el UID del usuario logueado
      allow list: if isSignedIn() && (
        request.auth.token.email == 'control@pcgoperacion.com' || 
        resource.data.userId == request.auth.uid || 
        isSuperAdmin()
      );
      allow get: if isSignedIn() && (
        request.auth.token.email == 'control@pcgoperacion.com' || 
        resource.data.userId == request.auth.uid || 
        isSuperAdmin()
      );
      allow update: if isSignedIn() && (
        request.auth.token.email == 'control@pcgoperacion.com' || 
        resource.data.userId == request.auth.uid || 
        isSuperAdmin()
      );

      match /messages/{messageId} {
        allow read: if isSignedIn() && (
          request.auth.token.email == 'control@pcgoperacion.com' || 
          get(/databases/$(database)/documents/support_tickets/$(ticketId)).data.userId == request.auth.uid || 
          isSuperAdmin()
        );
        allow create: if isSignedIn() && (
          request.auth.token.email == 'control@pcgoperacion.com' || 
          get(/databases/$(database)/documents/support_tickets/$(ticketId)).data.userId == request.auth.uid || 
          isSuperAdmin()
        );
      }
    }

    match /bids/{bidId} {
      allow read: if true;
      allow write: if isSuperAdmin();
    }

    match /settings/{settingId} {
      allow read: if true;
      allow write: if isSuperAdmin();
    }
    
    match /mp_cache/{cacheId} {
      allow read, write: if isSuperAdmin();
    }
  }
}
